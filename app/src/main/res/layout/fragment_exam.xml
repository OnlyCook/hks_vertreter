<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?attr/homeworkFragmentBackgroundColor">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp"
        android:background="?attr/homeworkFragmentBackgroundColor">

        <!-- search -->
        <EditText
            android:id="@+id/searchBarExam"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:hint="@string/frag_exam_search_hint"
            android:inputType="text"
            android:padding="12dp"
            android:background="@drawable/subtle_search_background"
            android:drawableStart="@android:drawable/ic_menu_search"
            android:textSize="16sp"
            android:textColorHint="#757575"
            android:drawablePadding="8dp"
            android:paddingStart="20dp"/>

        <Button
            android:id="@+id/btnAddExam"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="＋"
            android:textSize="18sp"
            android:textStyle="bold"
            android:backgroundTint="@android:color/holo_green_light"
            android:minWidth="48dp"
            android:layout_marginEnd="8dp"
            style="@style/Widget.AppCompat.Button" />

        <Button
            android:id="@+id/btnMenuExam"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="☰"
            android:textSize="18sp"
            android:backgroundTint="@android:color/holo_blue_light"
            android:minWidth="48dp"
            style="@style/Widget.AppCompat.Button" />

    </LinearLayout>

    <!-- header with counter -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:background="?attr/homeworkFragmentBackgroundColor"
        android:gravity="center_vertical">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/frag_exam_your_exams"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="?attr/homeworkFragmentTextColor"/>

        <TextView
            android:id="@+id/tvExamCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/frag_exam_count_format"
            android:textSize="14sp"
            android:textColor="?attr/homeworkFragmentTextColor" />

    </LinearLayout>

    <!-- Exam list -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewExam"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingHorizontal="8dp"
        android:paddingBottom="16dp"
        android:clipToPadding="false"
        android:scrollbars="vertical" />

</LinearLayout>