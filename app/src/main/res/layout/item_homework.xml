<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:layout_marginVertical="4dp"
    android:layout_marginHorizontal="8dp"
    android:background="@drawable/rounded_background"
    android:elevation="2dp">

    <!-- Checkbox area (for checking/unchecking) -->
    <FrameLayout
        android:id="@+id/checkboxArea"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:padding="8dp">

        <CheckBox
            android:id="@+id/checkBoxHomework"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:clickable="false"
            android:focusable="false"/>
    </FrameLayout>

    <!-- Text area (for viewing details) -->
    <TextView
        android:id="@+id/textHomeworkDetails"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_gravity="center_vertical"
        android:text="Mathe | heute fällig"
        android:textSize="16sp"
        android:padding="8dp"
        android:background="?android:attr/selectableItemBackground"/>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_gravity="center_vertical">

        <ImageButton
            android:id="@+id/btnEdit"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_pencil"
            android:backgroundTint="@android:color/holo_orange_light"
            android:layout_marginEnd="4dp"
            android:scaleType="centerInside"
            android:contentDescription="@string/item_hw_content_description_edit"
            style="@style/Widget.AppCompat.Button" />


        <ImageButton
            android:id="@+id/btnDelete"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_trash_can"
            android:backgroundTint="@android:color/holo_red_dark"
            android:scaleType="centerInside"
            android:contentDescription="@string/item_hw_content_description_delete"
            style="@style/Widget.AppCompat.Button" />

    </LinearLayout>

</LinearLayout>